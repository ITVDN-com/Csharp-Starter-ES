using System;

// Variable (Variable) - es la parte  de la memoria que almacena un valor determindado que se puede cambiar .

namespace TypeBasics
{
    class Program
    {
        static void Main()
        {
            // En la linea 13 , declaramos una variable de nombre a , tipo byte y le asignamos  el valor de 2.
            
            byte a = 2;

            // En la linea 17 , sacamos el valor de la variable   - a, en la pantalla.

            Console.WriteLine(a);

            // Demora.
            Console.ReadKey();
        }
    }
}
